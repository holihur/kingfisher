<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kingfisher.modules.department.mapper.DepartmentMapper">
    <resultMap id="DeptMap" type="com.kingfisher.modules.department.domain.Department">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <result property="sort" column="sort"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="version" column="version"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="DeptMap">
        SELECT id, parent_id, name, sort, status, remark, version, created_at, updated_at FROM departments ORDER BY sort ASC
    </select>

    <select id="findById" resultMap="DeptMap">
        SELECT id, parent_id, name, sort, status, remark, version, created_at, updated_at FROM departments WHERE id = #{id}
    </select>

    <select id="findByParentId" resultMap="DeptMap">
        SELECT id, parent_id, name, sort, status, remark, version, created_at, updated_at FROM departments WHERE parent_id = #{parentId} ORDER BY sort ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO departments (parent_id, name, sort, status, remark, version, created_at, updated_at)
        VALUES (#{parentId}, #{name}, #{sort}, #{status}, #{remark}, #{version}, datetime('now'), datetime('now'))
    </insert>

    <update id="update">
        UPDATE departments SET name=#{name}, parent_id=#{parentId}, sort=#{sort}, status=#{status}, remark=#{remark}, version=#{version}, updated_at=datetime('now') WHERE id=#{id}
    </update>

    <delete id="deleteById">DELETE FROM departments WHERE id=#{id}</delete>

    <select id="countChildren" resultType="long">SELECT COUNT(*) FROM departments WHERE parent_id = #{parentId}</select>

    <select id="findRoleIdsByDeptId" resultType="long">SELECT role_id FROM department_roles WHERE department_id = #{deptId}</select>
    <delete id="deleteDeptRoles">DELETE FROM department_roles WHERE department_id = #{deptId}</delete>
    <insert id="insertDeptRole">INSERT INTO department_roles (department_id, role_id) VALUES (#{deptId}, #{roleId})</insert>
</mapper>
