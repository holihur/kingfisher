<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kingfisher.modules.dict.mapper.DictEntryMapper">
    <resultMap id="DictEntryMap" type="com.kingfisher.modules.dict.domain.DictEntry">
        <id property="id" column="id"/>
        <result property="typeId" column="type_id"/>
        <result property="label" column="label"/>
        <result property="value" column="value"/>
        <result property="sort" column="sort"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="version" column="version"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="whereClause">
        <where>
            type_id = #{typeId}
            <if test="keyword != null and keyword != ''">
                AND (label LIKE '%' || #{keyword} || '%' OR value LIKE '%' || #{keyword} || '%' OR remark LIKE '%' || #{keyword} || '%')
            </if>
            <if test="filters != null">
                <foreach collection="filters" item="f">
                    <choose>
                        <when test="f.op.name() == 'EQ'">AND ${f.field} = #{f.value}</when>
                        <when test="f.op.name() == 'NE'">AND ${f.field} != #{f.value}</when>
                        <when test="f.op.name() == 'CONTAINS'">AND ${f.field} LIKE '%' || #{f.value} || '%'</when>
                    </choose>
                </foreach>
            </if>
        </where>
    </sql>

    <select id="findByTypeId" resultMap="DictEntryMap">
        SELECT id, type_id, label, value, sort, status, remark, version, created_at, updated_at FROM dict_entries
        <include refid="whereClause"/>
        ORDER BY
        <choose><when test="sort != null and sort != ''">${sort}</when><otherwise>sort ASC, id DESC</otherwise></choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByTypeId" resultType="long">
        SELECT COUNT(*) FROM dict_entries <include refid="whereClause"/>
    </select>

    <select id="findPublicByCode" resultMap="DictEntryMap">
        SELECT e.id, e.type_id, e.label, e.value, e.sort, e.status, e.remark, e.version, e.created_at, e.updated_at
        FROM dict_entries e JOIN dict_types t ON t.id = e.type_id
        WHERE t.code = #{code} AND t.is_public = 1 AND e.status = 1
        ORDER BY e.sort ASC
    </select>

    <select id="findById" resultMap="DictEntryMap">
        SELECT id, type_id, label, value, sort, status, remark, version, created_at, updated_at FROM dict_entries WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dict_entries (type_id, label, value, sort, status, remark, version, created_at, updated_at)
        VALUES (#{typeId}, #{label}, #{value}, #{sort}, #{status}, #{remark}, #{version}, datetime('now'), datetime('now'))
    </insert>

    <update id="update">
        UPDATE dict_entries SET label=#{label}, value=#{value}, sort=#{sort}, status=#{status}, remark=#{remark}, version=#{version}, updated_at=datetime('now') WHERE id=#{id}
    </update>

    <delete id="deleteById">DELETE FROM dict_entries WHERE id=#{id}</delete>

    <delete id="deleteBatch">
        DELETE FROM dict_entries WHERE id IN <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>

    <update id="updateStatusBatch">
        UPDATE dict_entries SET status=#{status}, updated_at=datetime('now') WHERE id IN <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </update>
</mapper>
