FROM node:20-alpine AS builder
WORKDIR /app
COPY kingfisher-web/package.json kingfisher-web/package-lock.json ./
RUN npm ci
COPY kingfisher-web/ .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY deploy/nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80